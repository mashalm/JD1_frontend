logger

Form validation before submit/login/etc are clicked

When signing up, check to see if email already exists

Fix up alert UI -- give more feedback of what the backend is doing on the front end
